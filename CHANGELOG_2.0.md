# 3.0.7 — контроль конфликтов NAT-портов

- Поиск клиента теперь проверяет дубли внешних TCP dst-port на его VPN-сервере.
- Конфликт сразу виден в результатах и карточке клиента.
- Показывается другая учётка или Remote Address, использующая тот же порт.
- Конфликтные порты подсвечиваются в списке.
- Автоматическая миграция клиентов между VPN исключена из roadmap.

# 3.0.7 — Темы снова разные

- Отменена унификация всех тем вокруг одного розового акцента.
- Каждая тема получила собственный accent/hover/selection/progress/calendar/focus цвет.
- Розовое молочко остаётся фирменно-розовой.
- Светлая LinkVideo теперь с синим акцентом.
- Лавандовая — с фиолетовым.
- Тёмно-синяя — с цианово-голубым.
- Полуночная — с насыщенным фиолетовым.
- Тёмная вишня — с малиново-вишнёвым.
- Графитовая — с тёплым золотым акцентом.
- Семантические success/warning/danger остаются предсказуемыми между темами.

# 3.0.7 — VPN Runtime, ожидание и темы

- фоновые операции вынесены в модальное окно ожидания;
- исправлен и верифицируется запуск/остановка LV Scheduler;
- добавлены run-count задач LV;
- темы унифицированы и полностью переведены на русский;
- сохранена бизнес-логика 3.0.3 архива и VPN.

# 3.0.2 — Theme Polish

- Цветовые темы приведены к единому современному design system.
- Названия тем переведены на русский; удалены «Персиковая» и «Лесная».
- Нативная палитра Qt синхронизируется с QSS, поэтому календарь, switch, spinner и custom-painted элементы корректно реагируют на тему.
- Убраны жёстко заданные розовые/серые цвета из рисуемых элементов.
- Акцентный блок портов/учёток и рабочая карточка клиента получили более подходящие поверхности.

# 3.0.1 — UI Polish

- Возвращена отдельная тема «Розовое молочко».
- LinkVideo 2026 и LinkVideo Light снова визуально различаются.
- Исправлен календарь в тёмных темах: фон, даты и выделение теперь читаемы.
- В новых периодах архива секунды по умолчанию выставляются в 00.
- Исправлено обрезание карточек результатов поиска.
- Карточка клиента теперь использует всю высоту рабочей области; убрана пустая половина страницы.
- Список найденных клиентов в компактном режиме показывает короткую стабильную строку без обрезания.

# 3.0.1 — UI Foundation

- Новый визуальный каркас LinkVideo.Helper.
- Современная нейтральная светлая тема LinkVideo 2026.
- Переработаны shell/sidebar, карточки, кнопки, поля, таблицы, статусы и scrollbar.
- Создание клиента переведено в современный двухпанельный workspace с сохранением портов/учёток в одной строке.
- Сохранены встроенная карточка клиента, VPN automation, backup и архивные функции 3.0.1.
- Зафиксирована схема будущей полной синхронизации RouterOS → Google Sheets.

# LinkVideo.Helper 3.0.1

## 3.0.1
- Карточка клиента открывается внутри страницы, без отдельного окна.
- Панель найденных клиентов плавно сжимается влево и остаётся доступной для быстрого переключения.
- Выбранный порт отображается одной плашкой; дублирующие подписи удалены.
- Сортировка VPN-клиентов переведена на современные сегменты «Старые / Новые / По серверу».
- Добавлена полная ручная выгрузка выбранного VPN-сервера отдельно от выгрузки всех серверов.
- Индикатор прогресса увеличен до 10 px, чтобы не обрезаться в Windows.


- Счётчики «Количество портов» и «Количество учёток» снова всегда находятся в одной строке.
- Страница «Поиск и управление» стала компактной: основное окно содержит только поиск и результаты.
- Карточка клиента и все действия с учёткой открываются отдельным окном по клику на найденную запись.
- После добавления/удаления/переключения портов карточка и строка результата синхронизируются автоматически; ручной перезаход на страницу не требуется.
- Новые NAT-порты по-прежнему выделяются отдельно и могут быть скопированы одной кнопкой.
- Сортировка VPN-клиентов заменена на понятный выпадающий список: «Сначала самые старые», «Сначала самые новые», «По VPN-серверу».
- Исправлен индикатор фоновой проверки: процент больше не обрезается внутри тонкой полосы прогресса.
- Таблица VPN-серверов получила собственную фиксированную область прокрутки; колесо мыши над списком больше не двигает всю страницу.
- Сохранены full backup, LV Automation, карантин/автовосстановление, балансировка L2TP, автообновление клиента и исправления архива.

# LinkVideo.Helper 2.1.2

- Полный запуск/остановка LV-автоматики на одном VPN или сразу на всех.
- Пауза отключает LV-Activity, LV-AutoRestore, LV-Aging и служебные logging rules, но не удаляет скрипты и не меняет PPP Secret.
- При запуске восстанавливается состояние автокарантина, которое было до паузы.
- Обновление LV на остановленном сервере больше не запускает автоматику самопроизвольно.
- Все состояния VPN-клиентов переведены на русский.
- Добавлена памятка состояний на странице VPN-серверов.
- В таблице и карточках удалены пользовательские A/S/Q/R/M/U, Last Seen, Lifecycle, Q ON/OFF.

# LinkVideo.Helper 2.1.1

- Добавлена автономная LV Automation для VPN-серверов: LV-Activity, LV-AutoRestore и LV-Aging устанавливаются/обновляются через RouterOS API.
- Перед установкой LV на сервер Helper автоматически создаёт FULL backup его конфигурации.
- Добавлена массовая безопасная установка/обновление LV на всех включённых VPN; карантин при этом нигде автоматически не включается.
- Добавлена инициализация Last Seen без Disable/Enable: активные учётки получают A, 30+ дней — S, 90+ — Q, 365+ — R, старые ручные disabled — M, неизвестные — U.
- LV-Aging после первой установки всегда disabled и включается только отдельным подтверждением Q ON.
- LV-AutoRestore реагирует только на Q/R и не может отменить ручное отключение M.
- Для auth-failure создаётся отдельный RAM logging buffer LV-Auth; диск RouterOS не используется.
- Старый раздел «Архив клиентов» переделан в «VPN-клиенты» с lifecycle-состояниями S/Q/R/M/U.
- Ручное включение/отключение из Helper синхронизирует lifecycle: Enable -> A, Disable -> M.
- Добавлены регрессионные тесты на lifecycle marker, безопасную установку Aging=OFF, сохранение ручного M и seed без изменения disabled-флагов.

## LinkVideo.Helper 2.1.0

- Добавлен раздел «VPN-серверы» с Active L2TP/500, PPP Secrets, CPU, RAM и NAT.
- Автовыбор новых клиентов теперь исключает VPN с 450+ активными L2TP и выбирает сервер прежде всего по числу активных L2TP.
- Добавлена полная резервная выгрузка всех VPN: PPP Secret с паролями, PPP profiles, NAT/filter/mangle/raw, pools, addresses, routes, scripts/scheduler/logging и manifest SHA-256.
- Исправлено противоречие «Архив найден частично: 0 сек» — нулевой результат теперь всегда считается отсутствием архива.
- Добавлен HTTPS HLS fallback: если Nimble management :8086 не отдаёт DVR timeline, Helper проверяет реальные playlist_dvr_range/chunks и разрешает скачивание, когда медиасегменты существуют.
- В архиве метрика показывает число реально проверенных DVR-серверов; для старого периода выводится подсказка о глубине хранения, если B2O её сообщил.

## 2.0.2 — стабильность обновлений, MikroTik и скачивания архива

- Возвращена автоматическая проверка обновлений после запуска Helper; сетевые ошибки фоновой проверки не мешают работе.
- Обновлятор больше не запускает скачанный файл вслепую: проверяется Windows EXE, ProductVersion и, если он опубликован, SHA-256.
- `build_setup.bat` генерирует `release_upload/version.json` по уже собранному и проверенному Setup и автоматически записывает SHA-256.
- Исправлено сравнение версий: `2.0.2` и `2.0.2.0` считаются одной версией.
- Создание VPN-клиента стало транзакционным: если PPP Secret/NAT не создался или операция отменена, уже созданный PPP Profile также откатывается.
- Массовое добавление NAT-портов откатывает уже созданные правила, если один из последующих портов не добавился.
- `Пересоздать порт` восстанавливает прежнее NAT-правило, если создание нового завершилось ошибкой.
- Генерация VPN-паролей переведена с `random` на `secrets`.
- Кэшированный FFmpeg теперь реально проверяется через `ffmpeg -version`; повреждённая копия в LocalAppData удаляется и скачивается заново.
- Esc/отмена теперь действует и во время fallback без аудио, и во время объединения нескольких частей FFmpeg.
- Повторное скачивание одинакового периода больше не перезаписывает ранее сохранённый MP4: создаётся новое имя с суффиксом.
- Чистая сборка Setup сохраняет единый AppId и перед установкой удаляет runtime старой версии, не затрагивая настройки и `Videos\LinkVideo_Archive`.
- Добавлены регрессионные тесты на rollback PPP Profile/NAT и восстановление NAT при неудачном пересоздании.

## 2.0.1 — исправление скачивания архива

- Исправлена ошибка `[WinError 5] Отказано в доступе: C:\Program Files\LinkVideo.Helper\tools`.
- FFmpeg больше не записывается в Program Files; загружаемая копия хранится в `%LOCALAPPDATA%\LinkVideo.Helper\tools`.
- Добавлен корректный поиск FFmpeg, встроенного в PyInstaller, старого расположения и системного PATH.
- Недокачанный FFmpeg больше не может подменить рабочий файл.

## 2.0.0 RC09 — архив клиентов и региональные архивные кластеры

- Добавлен раздел «Архив клиентов» для VPN-учёток с подтверждённым last-logged-out старше 183 дней.
- В архиве клиентов можно включать/отключать и удалять выбранную учётную запись.
- Удаление клиента из обычного поиска больше не очищает остальные найденные результаты.
- Кнопка «Настройки» центрирована в боковой панели.
- Выбранный клиент и NAT-порт подсвечиваются заметнее.
- Диагностика архива: оператор вынесен в отдельную строку, ID камеры расположен ниже.
- Зафиксированы региональные архивные профили: Казахстан (1721, cluster linkvideokz) и Беларусь (1741, cluster linkvideoby).
- Добавлены встроенные региональные архивные серверы и возможность расширять их список в настройках без новой версии программы.
- Глобальная история найденных vcore разделена по операторам, чтобы серверы разных стран не смешивались.
- Региональный fallback больше не перебирает российский пул b2o-vcore1..500.


## RC07 — окно, прокрутка и региональный RouterOS API

- Стартовый размер окна LinkVideo.Helper: 1220×910.
- Версия программы центрируется под кнопкой «Настройки».
- Кнопка «Копировать данные» в результате создания перенесена влево.
- Рабочие страницы используют scroll-area с минимальной desktop-компоновкой: при уменьшении окна блоки больше не накладываются друг на друга, а интерфейс прокручивается.
- RouterOS API при быстром `connection refused` на 8728 автоматически пробует API-SSL 8729 и запоминает рабочий стандартный API-порт на время запуска. Если оба API-порта закрыты, но WinBox 8291 доступен, Helper прямо сообщает, что проблема именно в сервисе RouterOS API, а не в учётных данных WinBox.
- WinBox 8291 остаётся отдельным сервисом и не используется как RouterOS API.

# LinkVideo.Helper 2.0.0

## Интерфейс

- Полностью новый визуальный стиль при сохранении функционального расположения элементов LinkVideo.Helper 1.1.2.
- Responsive desktop-workbench: рабочие страницы используют окно целиком, но на широких экранах получают умеренные боковые отступы.
- Возвращены темы старого Helper.
- Добавлены анимации переходов, состояния ожидания, успешного действия, ошибки и визуальная обратная связь после копирования.
- Убран нативный flash при первом открытии вкладок за счёт предварительного создания страниц после показа главного окна.
- Новый выбор даты/времени без Windows spinbox; дата и время отображаются по центру поля.
- Настройки больше не ограничены узкой центральной колонкой.

## VPN / MikroTik

- Встроенные серверы: vpn01–vpn10 (Россия), rb-vpn01 (Беларусь), kz-vpn01 (Казахстан).
- Беларусь и Казахстан доступны при ручном выборе; автоматический подбор нагрузки использует только российские VPN-серверы.
- Пользовательские VPN-серверы сохраняются и могут включаться/отключаться без новой версии программы.
- Параллельный поиск по логину и поиск по внешнему порту на выбранном сервере.
- Поиск по Remote IP удалён.
- Автоматическая подстановка следующего свободного логина при конфликте сохранена.
- Точечные RouterOS API-запросы вместо полного snapshot там, где это возможно.
- Ошибки auth/timeout/DNS/API разделены и не подменяются значениями CPU=0 или ложным «клиент не найден».
- Управление клиентом: пароль, PPP Secret, NAT-порты, отключение текущей VPN-сессии, копирование всех данных одной кнопкой.
- Отдельный статус трафика NAT-порта удалён как недостоверный. Общий RX/TX VPN и мониторинг сохранены.

## Архив

- Отдельный ArchiveService вместо логики внутри главного окна.
- Автоматическое получение server_name, stream, wmsAuthSign и timezone из B2O.
- Поиск reserve-transfers и истории vcore за выбранный период.
- Проверка Nimble DVR timeline и скачивание всех подтверждённых участков.
- Подписанные playlist_dvr_range URL с wmsAuthSign.
- Реальный прогресс FFmpeg через `-progress` без вывода полного технического лога пользователю.
- Пропуски исходного DVR не отменяют скачивание.
- Период вводится во времени камеры; reserve-transfers сопоставляется как UTC+7 через epoch.

## Диагностика архива

- Точные DVR-интервалы и разрывы.
- Сравнение нескольких камер одного адреса.
- Сравнение с другими камерами текущего vcore для поиска массовой серверной проблемы.
- Отдельная проверка переездов.
- Автоматический движок причины разрывов различает: конкретную камеру/локальное подключение, весь адрес клиента, сервер/vcore, переезд, недостаточно данных.
- Для вывода показывается степень уверенности и факты, на которых основано решение.
- Если доказательств недостаточно, Helper не придумывает причину.

## Установка и обновление

- Название приложения, EXE и ярлыка: LinkVideo.Helper.
- Версия: 2.0.0.
- Сохранён AppId 1.1.2, поэтому Setup обновляет существующую установку.
- Сохранён Google Drive manifest обновлений.
- Перед release-сборкой выполняются sync version, self-check и core tests.
- Приватные SSH-ключи, старые patch/server-maintenance файлы и лишние кэши не входят в релиз.

## 2.0.0 RC06 — финальная полировка перед выпуском

- Карточка клиента возвращена к функциональной компоновке 1.1.2 при сохранении нового визуального стиля.
- Исправлено отображение списка NAT-портов: каждый порт снова занимает отдельную компактную строку.
- Результат создания клиента возвращён к единому текстовому блоку 1.1.2 с одной кнопкой копирования.
- B2O-статус перенесён над выбором периода в скачивании и диагностике архива.
- Date/time popover закрывается кликом вне календаря; сохранён удобный прямой ввод ЧЧ:ММ:СС.
- Esc отменяет создание клиента, поиск, поиск/скачивание архива и диагностику. FFmpeg при отмене останавливается.
- Успешный поиск больше не показывает лишнюю плашку «Клиент найден».
- Ошибка TCP connection refused формулируется как недоступность RouterOS API; при найденном клиенте сбой отдельного регионального сервера не перекрывает результат.
- Диагностический движок стал строже: случайные короткие пересечения разрывов больше не считаются массовым сбоем; учитывается доля пересечения и синхронность начала.
- В вывод диагностики добавлены ограничения по непроверенным камерам, чтобы отсутствие данных не трактовалось как исправность.

## 2.0.0 — финальный RC08

- Логин при создании теперь проверяется и дополняется суффиксом только на выбранном MikroTik; записи на других VPN-серверах больше не заставляют Казахстан получать `_3`.
- Поиск по базовому логину на всех VPN-серверах также находит его серверные варианты `_1`, `_2`, `_3` и поэтому корректно показывает региональные учётки.
- Выбранный клиент и выбранный NAT-порт получили более заметное состояние выбора.
- Зафиксированы операторы B2O: Россия — `241` / `linkvideo_`, Казахстан — `1721` / `linkvideokz_`, Беларусь — `1741` / `linkvideoby_`.
- В скачивание архива и диагностику добавлен фиксированный выбор оператора; числовые ID операторов не редактируются вручную.
- При вставке `linkvideokz_*` или `linkvideoby_*` оператор выбирается автоматически; для числового ID используется выбранная страна.
- История найденных DVR-vcore теперь хранится по полному имени камеры с префиксом страны, чтобы одинаковые числовые ID разных операторов не пересекались.


## RC10 — страны без ID и автоматические медиа-серверы
- В интерфейсе архива/диагностики/настроек показываются только страны: Россия, Казахстан, Беларусь. Внутренние ID B2O остаются внутри логики программы.
- Диагностика разделена визуально на блоки «Камера» и «Период проверки».
- Helper умеет получать актуальный список DVR/Nimble-серверов через `/operators/{operator}/dvr-servers` (вкладка «Медиа серверы»), кэширует его и использует при поиске архива.
- Ручное добавление архивных серверов сохранено как резервный механизм.
- В настройках добавлена кнопка обновления списка медиа-серверов из B2O.

### RC11 — Архив клиентов
- Добавлены сортировки: старые/новые, по серверу и по логину.
- Добавлен явный множественный выбор учётных записей с индикатором количества.
- Добавлены «Выбрать все» и «Снять выбор».
- Добавлены массовые действия: включить, отключить и удалить выбранные учётки.
- Массовые операции выполняются в фоне с прогрессом и частичным результатом при ошибках отдельных серверов.
- Выбранные для массового действия карточки имеют отдельную визуальную подсветку.


### RC12 — Архив клиентов: современный выбор и компоновка
- Стартовый размер окна изменён на 1230×930.
- Вкладка «Архив клиентов» визуально переработана: меньше служебных полос, более современный список и карточка деталей.
- Сортировки переименованы в «Дольше без входа», «Ближе к 6 месяцам» и «По серверу»; сортировка по логину удалена из интерфейса.
- Множественный выбор теперь работает стандартно: обычный клик выбирает одну запись, Ctrl+ЛКМ добавляет или убирает записи из выбора.
- Чекбоксы у каждой учётки убраны; выбранные записи подсвечиваются самой карточкой.
- Массовые действия сохранены: включение, отключение и удаление.


## 3.0.3 — быстрый маршрут архива
- player playlist стал первым источником определения архива;
- actual DVR host извлекается из URI медиасегментов выбранного периода;
- удалён brute-force b2o-vcore1..500;
- fallback ограничен реальным реестром B2O и изученной историей;
- скрыт служебный список «Архивные серверы» из настроек;
- обновлены статусы страницы скачивания архива.


### 3.0.7 startup hotfix
- Исправлен запуск после переработки тем: экранированы фигурные скобки BusyDialog QSS.
- Добавлен runtime-тест рендера QSS для каждой темы, чтобы NameError внутри f-string больше не проходил релизную проверку.


## 3.0.7 — восстановление LV Automation
- Статус установки теперь учитывает скрипты, Scheduler и logging, а не только /system script.
- Частичная установка отображается как «Установлено частично», а не «Остановлено».
- «Запустить LV» автоматически восстанавливает недостающие Scheduler/logging-компоненты старых установок.
- После установки выполняется строгая повторная проверка полного комплекта.
- Расширены policy для служебных Script/Scheduler.
